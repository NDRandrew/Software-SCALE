/**
 * Arquivo de configuração do Cypress para testes de interface do usuário.
 * Este arquivo configura o Cypress para testar a aplicação Emergy.
 * 
 * Feito por André Carbonieri Silva T839FC9
 */

module.exports = {
  e2e: {
    baseUrl: 'http://localhost:5000',
    setupNodeEvents(on, config) {
      // implementar event listeners do node aqui
    },
  },
};
